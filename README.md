# unit-testing

This is a repository on unit testing with JavaScript

## Installation

- Install all neccessary development packages with `npm install --save-dev` (`-D`)

## Development

- Run tests with `npm run test`
